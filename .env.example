# Select groq or gemini. Set only the chosen provider's key locally.
LLM_PROVIDER=groq
GROQ_API_KEY=
GEMINI_API_KEY=
# Leave unset to use the selected provider's default model and API URL.
# LLM_MODEL=
# LLM_BASE_URL=
PORT=8000
